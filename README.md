# This is the official repo for  Practical methods for Approximate Nearest Neighbor Search on non-Manhattan squares

to install the package, run the following command in the terminal:

```bash
conda c
```
